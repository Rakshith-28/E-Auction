spring.application.name=eauction-backend
server.port=8080

# --- MongoDB Connection (FIXED) ---
spring.data.mongodb.uri=mongodb+srv://rakshithlk28:Rakshith%4028@cluster0.bifkqkw.mongodb.net/eauction?retryWrites=true&w=majority
spring.data.mongodb.database=eauction
spring.data.mongodb.auto-index-creation=true

# --- JWT ---
jwt.secret=change-me-in-production-please-123456
jwt.expiration-ms=86400000

# --- Mail (local fake mail server for dev) ---
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=username
spring.mail.password=password
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# --- Logging ---
logging.level.org.springframework.security=INFO
logging.level.com.eauction=DEBUG

# --- CORS ---
cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:3000

# --- Firebase ---
# Provide a Spring resource location (e.g. classpath:firebase/serviceAccount.json or file:C:/path/key.json)
# firebase.credentials.path=
firebase.credentials.path=file:/C:/keys/firebase-service-account.json

# --- File Upload ---
file.upload-dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB